export default class Cart {

  constructor(id, nome, restaurante, preco, image) {
    this.id = id
    this.nome = nome
    this.restaurante = restaurante
    this.preco = preco
    this.image = image
  }
}